{
  "framework": "nextjs",
  "buildCommand": "node scripts/prebuild.js && prisma generate && prisma db push --accept-data-loss && node scripts/seed-if-empty.js && next build",
  "installCommand": "npm install",
  "regions": ["fra1"]
}
