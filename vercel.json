{
  "framework": "nextjs",
  "buildCommand": "node scripts/prebuild.js && prisma generate && prisma migrate deploy && node scripts/seed-if-empty.js && next build",
  "installCommand": "npm install",
  "regions": ["fra1"]
}
